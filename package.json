{
  "name": "argus",
  "version": "3.1.0",
  "private": true,
  "description": "ARGUS — instant on-device object recognition: point the camera and objects are outlined and named on the feed. The full model suite (detection, 1000-class classification, OCR, pose) runs locally; nothing leaves the device.",
  "type": "module",
  "license": "SEE LICENSE IN LICENSE",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "serve": "node tools/serve.mjs",
    "start": "node tools/serve.mjs",
    "test": "node tests/run.mjs",
    "verify:models": "node tools/verify-models.mjs"
  },
  "keywords": [
    "on-device",
    "vision",
    "object-recognition",
    "onnx",
    "webgpu",
    "wasm",
    "pwa"
  ],
  "devDependencies": {
    "jpeg-js": "^0.4.4",
    "onnxruntime-web": "^1.30.0"
  }
}
